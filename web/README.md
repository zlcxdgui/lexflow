# LexFlow Web

Frontend em Next.js.

**Setup**
```bash
npm install
npm run dev
```

**Ambiente**
- Copie `web/.env.local.example` para `web/.env.local`
- Ajuste `NEXT_PUBLIC_API_URL` e `NEXT_PUBLIC_APP_URL`

Mais detalhes no `README.md` da raiz.
